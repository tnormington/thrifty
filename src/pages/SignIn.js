import React, { useState } from "react";

const SignIn = () => {
  return (
      <div>
          <h1>Sign In</h1>
            <form>
                <input type="text" value={email}
            </form>
      </div>
  );
};

export default Signin;
